[package]
name = "oc-core"
version = "0.1.0"
edition = "2021"

[dependencies]
async-trait = "0.1"
secrecy = { version = "0.8", features = ["serde"] }
serde = { version = "1", features = ["derive"] }
thiserror = "1"
uuid = { version = "1", features = ["serde", "v4"] }
